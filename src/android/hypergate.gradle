dependencies {
   // androidx transitives excluded: the SDK does not use androidx at runtime and
   // androidx.core 1.19.0 would force AGP 9.1+ onto consuming apps
   implementation('com.hypergate:sdk:1.6.0') {
       exclude group: 'androidx.core'
       exclude group: 'androidx.appcompat'
   }
}
